"use client"

import { useState } from "react"
import { Activity, BarChart3, Bell, BookOpen, CalendarDays, CheckCircle2, ChevronDown, ClipboardList, FileText, GraduationCap, LayoutDashboard, Menu, MessageSquare, Search, Settings, ShieldCheck, Sparkles, Users, X } from "lucide-react"

const navByRole = {
  Admin: ["Overview", "Students", "Teachers", "Courses", "Exams & grading", "Analytics"],
  Teacher: ["My dashboard", "My students", "Courses", "Assignments", "Grades", "Calendar"],
  Student: ["My overview", "My courses", "Assignments", "Study plan", "Messages", "My profile"],
} as const

const people = {
  Admin: { name: "Anna Magnusson", title: "Administrator", initials: "AM" },
  Teacher: { name: "Erik Johansson", title: "Teacher · Mathematics", initials: "EJ" },
  Student: { name: "Emma Andersson", title: "Student · SA22B", initials: "EA" },
}

const students = [["Emma Andersson", "SA22B", "Active", "92%", "Today"], ["Lucas Berg", "TE23A", "Active", "87%", "Yesterday"], ["Nora Lindqvist", "NA22C", "At risk", "61%", "2 days ago"], ["Oskar Nilsson", "EK23B", "Active", "95%", "3 days ago"]]

function AdminContent({ role, active }: { role: keyof typeof navByRole; active: string }) {
  const isTeacher = role === "Teacher"
  const isStudent = role === "Student"
  const title = isStudent ? "Your learning overview" : isTeacher ? "Teacher dashboard" : active
  const subtitle = isStudent ? "Keep track of your courses, assignments, and progress." : isTeacher ? "Everything you need to support your students today." : "A clear view of what is happening across your school."
  const cards = isStudent ? [["Course progress", "78%", "+8%", "this month"], ["Assignments due", "4", "2 soon", "next 7 days"], ["Study streak", "12 days", "+3 days", "personal best"], ["Current grade", "B+", "On track", "across courses"]] : isTeacher ? [["My students", "124", "+8", "this semester"], ["Active courses", "6", "+1", "this term"], ["Pending grading", "18", "5 urgent", "submissions"], ["Attendance", "91.4%", "+2.1%", "this month"]] : [["Total students", "1,248", "+6.4%", "from last month"], ["Active courses", "42", "+3", "this semester"], ["Completion rate", "84.7%", "+2.1%", "from last month"], ["Open interventions", "18", "-12.5%", "from last month"]]
  return <section className="mx-auto max-w-[1440px] p-4 md:p-8"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 text-sm text-primary">Thursday, 28 August 2026</p><h2 className="text-3xl font-semibold tracking-tight">{title}</h2><p className="mt-2 text-muted-foreground">{subtitle}</p></div><button className="flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90">{isStudent ? <><Sparkles className="size-4" /> Continue learning</> : <><ShieldCheck className="size-4" /> {isTeacher ? "Create assignment" : "Generate report"}</>}</button></div>
  <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value, change, note], i) => <div key={label} className="rounded-xl border border-border bg-card p-5"><div className="flex items-start justify-between"><p className="text-sm text-muted-foreground">{label}</p><span className="rounded-full bg-primary/10 px-2 py-1 font-mono text-[10px] text-primary">{change}</span></div><p className="mt-5 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{note}</p></div>)}</div>
  <div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_1fr]"><div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><h3 className="font-semibold">{isStudent ? "Weekly activity" : isTeacher ? "Class engagement" : "Learning activity"}</h3><p className="mt-1 text-sm text-muted-foreground">{isStudent ? "Your study time over the last 30 days" : "Engagement across your learning community"}</p></div><select className="rounded-lg border border-border bg-muted px-3 py-2 text-xs text-foreground"><option>Last 30 days</option><option>Last 90 days</option></select></div><div className="mt-8 flex h-48 items-end gap-2">{[35,48,42,63,54,70,62,81,66,74,88,78,92,76,84,68,79,90,83,96,72,86,91,77,89,94,80,87,98,90].map((height, i) => <div key={i} className="group flex-1"><div style={{ height: `${height}%` }} className="rounded-t-sm bg-primary/60 transition group-hover:bg-primary" /></div>)}</div><div className="mt-4 flex justify-between font-mono text-[10px] text-muted-foreground"><span>01 AUG</span><span>15 AUG</span><span>28 AUG</span></div></div>
  <div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><h3 className="font-semibold">{isStudent ? "Upcoming" : isTeacher ? "Needs attention" : "Needs attention"}</h3><p className="mt-1 text-sm text-muted-foreground">{isStudent ? "Your next learning moments" : "Items that need your attention"}</p></div><button className="text-xs text-primary hover:underline">View all</button></div><div className="mt-5 flex flex-col gap-4">{(isStudent ? [["MA", "Mathematics assignment", "Due tomorrow", "warn"], ["EN", "English · reading task", "Due Friday", "ok"], ["PH", "Physics lesson", "Continue course", "ok"]] : isTeacher ? [["NL", "Nora Lindqvist", "Attendance below 70%", "danger"], ["JK", "Johan Karlsson", "Missing 3 assignments", "warn"], ["ES", "Ella Sjöberg", "No activity in 7 days", "warn"]] : [["NL", "Nora Lindqvist", "Attendance below 70%", "danger"], ["JK", "Johan Karlsson", "Missing 3 assignments", "warn"], ["ES", "Ella Sjöberg", "No activity in 7 days", "warn"]]).map(([initials, name, reason, type]) => <div key={name} className="flex items-center gap-3"><div className={`grid size-9 place-items-center rounded-full text-xs font-semibold ${type === "danger" ? "bg-danger/15 text-danger" : type === "warn" ? "bg-accent/15 text-accent" : "bg-primary/15 text-primary"}`}>{initials}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{name}</p><p className="truncate text-xs text-muted-foreground">{reason}</p></div><CheckCircle2 className={`size-4 ${type === "danger" ? "text-danger" : "text-primary"}`} /></div>)}</div></div></div>
  <div className="mt-6 rounded-xl border border-border bg-card"><div className="flex flex-col gap-4 border-b border-border p-6 md:flex-row md:items-center md:justify-between"><div><h3 className="font-semibold">{isStudent ? "My courses" : isTeacher ? "My students" : "Recent students"}</h3><p className="mt-1 text-sm text-muted-foreground">{isStudent ? "Your enrolled courses and current progress" : "Latest activity across your student body"}</p></div><div className="flex gap-2"><button className="rounded-lg border border-border px-3 py-2 text-xs text-muted-foreground hover:bg-muted">Filter</button><button className="rounded-lg border border-border px-3 py-2 text-xs text-muted-foreground hover:bg-muted">{isStudent ? "View all courses" : "Export CSV"}</button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-muted/40 text-xs uppercase tracking-wider text-muted-foreground"><tr>{(isStudent ? ["Course", "Teacher", "Progress", "Next item"] : ["Student", "Class", "Status", "Progress", "Last active"]).map(h => <th key={h} className="px-6 py-3 font-mono font-normal">{h}</th>)}</tr></thead><tbody>{(isStudent ? [["Mathematics 2c", "Erik Johansson", "82%", "Assignment 4"], ["English 6", "Sara Lind", "76%", "Reading task"], ["Physics 1", "Mikael Berg", "68%", "Chapter 8"]] : students).map((row) => <tr key={row[0]} className="border-t border-border hover:bg-muted/30">{row.map((cell, i) => <td key={cell} className={`px-6 py-4 ${i === 0 ? "font-medium" : "text-muted-foreground"}`}>{cell}</td>)}</tr>)}</tbody></table></div></div></section>
}

export function AdminShell() { const [mobileOpen, setMobileOpen] = useState(false); const [role, setRole] = useState<keyof typeof navByRole>("Admin"); const [active, setActive] = useState<string>(navByRole.Admin[0]); const person = people[role]; const changeRole = (next: keyof typeof navByRole) => { setRole(next); setActive(navByRole[next][0]); setMobileOpen(false) }; return <div className="min-h-screen bg-background text-foreground"><aside className={`fixed inset-y-0 left-0 z-20 flex w-64 flex-col border-r border-border bg-[#0e1720] transition-transform lg:translate-x-0 ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}><div className="flex h-20 items-center justify-between border-b border-border px-6"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-xl bg-primary text-primary-foreground"><Activity /></div><div><p className="font-semibold tracking-tight">mindful</p><p className="font-mono text-[10px] uppercase tracking-[0.24em] text-muted-foreground">account console</p></div></div><button className="lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Close menu"><X /></button></div><nav className="flex flex-1 flex-col gap-1 p-4">{navByRole[role].map((label, i) => { const Icon = [LayoutDashboard, Users, BookOpen, ClipboardList, BarChart3, CalendarDays][i]; return <button key={label} onClick={() => { setActive(label); setMobileOpen(false) }} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm transition ${active === label ? "bg-primary/10 text-primary" : "text-muted-foreground hover:bg-muted hover:text-foreground"}`}><Icon className="size-4" /><span className="flex-1">{label}</span></button> })}</nav><div className="border-t border-border p-4"><div className="mb-3 flex gap-1 rounded-lg bg-muted/60 p-1">{(["Admin", "Teacher", "Student"] as const).map(item => <button key={item} onClick={() => changeRole(item)} className={`flex-1 rounded-md px-1 py-1.5 text-[10px] font-medium ${role === item ? "bg-primary text-primary-foreground" : "text-muted-foreground"}`}>{item}</button>)}</div><button className="flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-muted-foreground hover:bg-muted"><Settings className="size-4" /> Settings</button><div className="mt-4 flex items-center gap-3 rounded-lg bg-muted/60 p-3"><div className="grid size-8 place-items-center rounded-full bg-accent/20 text-sm font-semibold text-accent">{person.initials}</div><div className="min-w-0 flex-1"><p className="truncate text-sm">{person.name}</p><p className="text-xs text-muted-foreground">{person.title}</p></div><ChevronDown className="size-4 text-muted-foreground" /></div></div></aside>{mobileOpen && <button className="fixed inset-0 z-10 bg-black/50 lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Close navigation" />}<main className="lg:pl-64"><header className="sticky top-0 z-10 flex h-20 items-center gap-4 border-b border-border bg-background/90 px-4 backdrop-blur md:px-8"><button className="lg:hidden" onClick={() => setMobileOpen(true)} aria-label="Open menu"><Menu /></button><div className="flex-1"><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Workspace / {active}</p><h1 className="mt-1 text-xl font-semibold tracking-tight">Good morning, {person.name.split(" ")[0]}</h1></div><button className="hidden items-center gap-2 rounded-lg border border-border bg-card px-3 py-2 text-sm text-muted-foreground md:flex"><Search className="size-4" /> Search <kbd className="rounded bg-muted px-1.5 py-0.5 font-mono text-[10px]">⌘K</kbd></button><button className="relative rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Notifications"><Bell className="size-5" /><span className="absolute right-1.5 top-1.5 size-1.5 rounded-full bg-accent" /></button></header><AdminContent role={role} active={active} /></main></div> }
